<div id="projectDetails" class="panel">
    <div class="row">
        <div class="small-12 columns">
            <h2><?= $this->translate('Projekt Details') ?></h2>
            <?php if ($this->projects): ?>
                <div class="section-container vertical-tabs" data-section="vertical-tabs">
                    <?php foreach($this->projects as $i => $project): ?>
                        <?php $projectDetails = $project->getProjectDetails()->getFirst() ?>
                        <section class="section" data-section-region>
                            <p class="section-title" data-section-title="section-title">
                                <a href="#project-<?= $project->getId() ?>"><?= $projectDetails->getLabel() ?></a>
                            </p>
                            <div class="section-content" data-slug="section<?= $i ?>" data-section-content="section-content">
                                <h3><?= $projectDetails->getLabel() ?></h3>
                                <?= $this->partial('partials/project.attribs.phtml', array('project' => $project, 'projectDetails' => $projectDetails)) ?>
                            </div>
                        </section>
                    <?php endforeach ?>
                </div>
            <?php else: ?>
                <?= $this->translate('Es konnten keine Projekte zur Auswahl ermittelt werden.') ?>
            <?php endif; ?>
        </div>
    </div>
</div>

